{
  "devDependencies": {
    "sass": "^1.77.8",
    "sass-loader": "^16.0.0"
  },
  "scripts": {
    "watch": "sass --watch development/scss/:public/style --no-source-map",
    "build:sass": "sass development/scss/:public/style/ --no-source-map --style=compressed",
    "prod": "npm run build:sass"
  }
}
